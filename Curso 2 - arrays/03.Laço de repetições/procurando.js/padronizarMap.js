const nomes = ["ana Julia", "Caio vinicius", "BIA silva"];

/*
const nomesPadr = nomes.map((nome) => {
    return nome.toUpperCase();
})
*/

const nomesPadr = nomes.map((nome) => nome.toUpperCase());

console.log(nomesPadr);